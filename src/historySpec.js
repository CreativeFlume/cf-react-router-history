import { expect } from 'chai';
let history = require('../dist/history');

describe('history', () => {
  describe('history', () => {
    it('exports createHistory', () => {
      expect(history.createHistory).to.not.be.undefined;
    });
  });
});
